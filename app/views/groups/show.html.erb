<h2>Showing Group</h2>
<p id="notice"><%= notice %></p>
<p>
  <b>Name:</b>
  <%= @group.name %>
</p>
<p>
  <b>On Call:</b>
  <%= if not @group.person_id.nil? then Person.find(@group.person_id).name else "no one" end %>
</p>
<%= link_to 'Edit', edit_group_path(@group) %><br/><br/>
<%= link_to 'Back', groups_path %>
